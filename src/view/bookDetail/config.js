const bookDetailConfig = ['#f6f4ec', '#e8e8e8', '#d5bc83', '#9acfa1', '#2e5073', '#654e46']; // 背景色
const bookDetailColorConfig = ['#333', '#333', '#333', '#333', '#f5f5f5', '#928c8f']; // 前景色
const navItem = ['目录', '书签'];
const txtDatat = 'offsetHeight与offsetWidth作用相同，前者是获取元素的高度后者则是宽度．offsetHeight属性可以获取元素的可见高度值．包括边框和内部空白补丁．但超出元素的内容则不被计算．也就是说只返回元素的实际高度值．如果看得着边框．从上边框到下边框的距离则是offsetHeight返回的值．经过测试无论在IE或FF均返回正常．值得一提的是利用css来控制元素的高度，在IE里会被撑大,FF里则不会．但返回的值是正确的！offsetHeight虽不是W3C标准属性，但广泛被浏览器支持．;值得一提的是利用css来控制元素的高度，在IE里会被撑大,FF里则不会．但返回的值是正确的！offsetHeight虽不是W3C标准属性，但广泛被浏览器支持．offsetHeight与offsetWidth作用相同，前者是获取元素的高度后者则是宽度．offsetHeight属性可以获取元素的可见高度值．包括边框和内部空白补丁．但超出元素的内容则不被计算．也就是说只返回元素的实际高度值．如果看得着边框．从上边框到下边框的距离则是offsetHeight返回的值．经过测试无论在IE或FF均返回正常．值得一提的是利用css来控制元素的高度，在IE里会被撑大,FF里则不会．但返回的值是正确的！offsetHeight虽不是W3C标准属性，但广泛被浏览器支持．;值得一提的是利用css来控制元素的高度，在IE里会被撑大,FF里则不会．但返回的值是正确的！offsetHeight虽不是W3C标准属性，但广泛被浏览器支持．'
export {
    bookDetailConfig,
    bookDetailColorConfig,
    navItem,
    txtDatat
};
